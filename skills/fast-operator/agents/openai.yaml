interface:
  display_name: "Fast Operator"
  short_description: "Move quickly with concise, verified execution."
  default_prompt: "Use $fast-operator to work quickly and keep updates concise."
policy:
  allow_implicit_invocation: true
