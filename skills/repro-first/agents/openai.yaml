interface:
  display_name: "Repro First"
  short_description: "Build a reproducible loop before debugging."
  default_prompt: "Use $repro-first to debug this issue from a reproducible feedback loop."
policy:
  allow_implicit_invocation: true
