interface:
  display_name: "Clarify First"
  short_description: "Clarify scope and language before building."
  default_prompt: "Use $clarify-first to clarify this feature before implementation."
policy:
  allow_implicit_invocation: true
